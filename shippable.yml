
# Language setting
language: python

# Version number
python:
  - 2.6
  - 2.7
  - 3.1

env:
  matrix:
    - hoo=plk
  global:
    - test=env
  
build:
  ci:
    - python --version
    - echo "$hoo"
    - echo $test
