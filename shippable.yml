
# Language setting
language: python

# Version number
python:
  - 2.6
  - 2.7

env:
  matrix:
    - hoo=plk
  global:
    - test=env
  
build:
  ci:
    - python --version
    - apt-get install jq
    - echo "$hoo"
    - echo $test
    - echo lol
    - echo $COMMIT_MESSAGE | jq '.'
