
# Language setting
language: python

# Version number
python:
  - 2.6

env:
  matrix:
    - hoo=plk
  global:
    - test=env1
  
build:
  ci:
    - python -v
    - echo "$hoo"
    - echo $test
    - echo dfs
    - echo yaa
    - echo $COMMIT_MESSAGE
