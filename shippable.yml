
# Language setting
language: python

# Version number
python:
  - 2.6

env:
  matrix:
    - hoo=plk
  global:
    - test=env2
  
build:
  ci:
    - python -v
    - echo "$hoo"
    - echo $test
    - echo dfsd
    - echo yaad
    - echo $COMMIT_MESSAGE
