
# Language setting
language: python

# Version number
python:
  - 2.6

env:
  matrix:
    - hoo=plk
  global:
    - test=env1
  
build:
  ci:
    - python --version
    - echo "$hoo"
    - echo $test
    - echo dfsd
    - echo yaad
    - echo yaa
    - echo $COMMIT_MESSAGE
